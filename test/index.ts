import API from '../api/index.ts'

const host = "localhost"
const port = 8080
const root = `http://${host}:${port}`

// —————————————————————————————————————————————————————————————————————————————
// Workspace API

